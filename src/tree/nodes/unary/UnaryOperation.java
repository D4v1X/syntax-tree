package tree.nodes.unary;

import tree.nodes.Operation;

public class UnaryOperation extends Operation{

    public UnaryOperation(String name, char operator) {
        super(name, operator);
    }
}
